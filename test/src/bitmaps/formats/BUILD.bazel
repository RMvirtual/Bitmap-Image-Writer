load("@rules_cc//cc:defs.bzl", "cc_test")

cc_library(
    name = "fixtures",
    hdrs = glob(["*.h"]),
    srcs = glob(["*.cpp"]),
    deps = [
        "@com_google_googletest//:gtest_main",
        "//test/src/bitmaps/fixtures:test_fixtures",
        "//src/bitmaps/formats:bitmap_formats",
    ],
    visibility = ["//visibility:public"],
)

cc_test(
    name = "tests",
    size = "small",
    srcs = glob(["*.cpp"]),
    deps = [
        "@com_google_googletest//:gtest_main",
        "//test/src/bitmaps/formats:fixtures"
    ]
)