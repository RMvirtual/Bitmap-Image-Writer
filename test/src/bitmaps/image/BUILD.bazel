load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "tests",
    size = "small",
    srcs = glob(["*.h", "*.cpp"]),
    deps = [
        "@com_google_googletest//:gtest_main",
        "//src/bitmaps/image:bitmap_image"
    ]
)