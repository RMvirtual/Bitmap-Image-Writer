load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "tests",
    size = "small",
    srcs = glob(["*.cpp"]),
    deps = [
        "@googletest//:gtest_main",
        "@wx_widgets//:wx_widgets"
    ],
)