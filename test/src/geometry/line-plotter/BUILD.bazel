load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "line-plotter",
    size = "small",
    srcs = glob(["*.h", "*.cpp"]),
    deps = [
        "@googletest//:gtest_main",
        "//src/geometry",
    ],
)