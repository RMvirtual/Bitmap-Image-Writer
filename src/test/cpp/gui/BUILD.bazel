load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "gui",
    size = "small",
    srcs = glob(["*.h", "*.cpp"]),
    deps = [
        "@googletest//:gtest_main",
        "@python_windows//:python-lib"
    ],
)